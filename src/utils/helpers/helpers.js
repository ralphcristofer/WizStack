/**
 * @description Helper functions for the application.
 * @exports {Object} Functions to be used by the application.
 */

/**
 * For each function, provide internal documentation describing what the function does.
 * @function
 * @param {Object} req - The request object.
 * @param {Object} res - The response object.
 * @param {Object} next - The next middleware function.
 * @returns {Object} The response object.
 * @description This function does something.
 * @example
 * const { helperFunction } = require('./helpers');
 * 
 * const exampleFunction = (req, res, next) => {
 *  // Do something.
 * return res.status(200).json({ message: 'Success!' });
 * };
 */